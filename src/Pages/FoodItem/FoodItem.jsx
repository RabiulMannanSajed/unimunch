const FoodItem = ({ foodData }) => {
  //   const {} =FoodItem
  console.log(foodData);
  const { id, img, price, foodName, course, rating } = foodData;
  return <div>{/* TODO : Take the data and show in UI */}</div>;
};

export default FoodItem;
